# Платформа Коллекционеров

Веб-приложение для управления коллекционерами, их коллекциями и предметами каталога.

## Функциональность

- 📊 Аналитический дашборд с графиками и статистикой
- 👥 Управление коллекционерами (CRUD операции)
- 📚 Управление коллекциями (CRUD операции)
- 🗂️ Управление каталогом предметов (CRUD операции)
- 📈 Визуализация данных (диаграммы, графики)
- 📄 Генерация PDF отчетов
- 📊 Экспорт в Excel
- 🔄 Автоматическое обновление данных

## Технологии

- **Python 3.8+**
- **PySide6** - GUI фреймворк
- **aiomysql** - асинхронный доступ к MySQL
- **Matplotlib** - визуализация данных
- **XlsxWriter** - генерация Excel отчетов
- **PDFKit** - генерация PDF отчетов
- **Jinja2** - шаблонизация HTML

## Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/visikdarina8-cell/Collectors
cd project
```
2. Создание виртуального окружения
```bash

python -m venv venv
```
# Windows
```bash
venv\Scripts\activate
```
# Linux/Mac
```bash
source venv/bin/activate
```
3. Установка зависимостей
```bash

pip install -r requirements.txt
```
4. Настройка базы данных

    Установите MySQL

    Создайте базу данных is21-08

    Импортируйте структуру таблиц из файла database_schema.sql

    Настройте подключение в файле .env

5. Настройка wkhtmltopdf (для PDF отчетов)

    Windows: Скачайте и установите wkhtmltopdf из официального сайта

    Linux: sudo apt-get install wkhtmltopdf

    Mac: brew install wkhtmltopdf

Запуск приложения
```bash

# Активация виртуального окружения
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate

# Запуск приложения
python -m src.main
```
Структура проекта
```text

project/
├── run.py
├── src/                    # Исходный код
│   ├── __init__.py        # Инициализация пакета
│   ├── main.py            # Точка входа
│   ├── config.py          # Конфигурация
│   ├── database.py        # Работа с БД
│   ├── models.py          # Диалоговые окна
│   ├── reports.py         # Генерация отчетов
│   └── ui.py              # Графический интерфейс
├── templates/             # HTML шаблоны
├── tests/                 # Тесты
├── requirements.txt       # Основные зависимости
├── requirements-dev.txt   # Доп. зависимости
├── .env.example          # Шаблон переменных окружения
└── README.md             # Документация
```
Конфигурация

Создайте файл .env на основе .env.example:
```env

DB_HOST=localhost
DB_PORT=3306
DB_USER=darina
DB_PASSWORD=1308
DB_NAME=is21-08
```
Использование
Главное меню

    📊 Дашборд - аналитика и статистика

    👥 Коллекционеры - управление коллекционерами

    📚 Коллекции - управление коллекциями

    🗂️ Каталог - управление предметами

    📊 Отчет в Excel - экспорт данных в Excel

    📊 PDF Отчеты - генерация PDF отчетов

    ℹ️ О платформе - информация о приложении

Операции с данными

    Добавить - создание новой записи

    Редактировать - изменение существующей записи

    Удалить - удаление записи (с учетом внешних ключей)

    Обновить - обновление данных из БД

Генерация отчетов

    PDF отчеты:

        Статистический отчет - общая статистика системы

        Детальный отчет - таблицы с подробными данными

    Excel отчет:

        Полный отчет со всеми данными

        Аналитика и визуализация

        Инфографика и выводы

Тестирование
```bash

# Установка dev-зависимостей
pip install -r requirements-dev.txt

# Запуск тестов
pytest tests/

# Запуск тестов с покрытием
pytest tests/ --cov=src

# Проверка стиля кода
flake8 src/
black --check src/
isort --check-only src/
```
Разработка
Установка для разработки
```bash

pip install -r requirements-dev.txt
pre-commit install
```
Форматирование кода
```bash

black src/
isort src/
```
